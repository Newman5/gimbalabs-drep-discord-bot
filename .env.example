# Copy to .env and fill real values. Do NOT commit .env with secrets.
BLOCKFROST_API_URL=https://blockfrost-m1.demeter.run
API_KEY=your_blockfrost_api_key_here
DISCORD_TOKEN=your_discord_bot_token_here
CHANNEL_ID=your_discord_channel_id_here

# Optional for local testing: use the local sample JSON instead of calling the API
USE_SAMPLE_PROPOSALS=false